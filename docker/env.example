# Hinge Automation Environment Configuration
# Copy this file to ../.env in the project root and fill in your actual values

# Google Gemini API Key (required)
# Get your free key from: https://aistudio.google.com/
# Note: Free tier has rate limits - consider paid tier for production use
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Custom device IP for ADB connection (default: 127.0.0.1)
# DEVICE_IP=192.168.1.100

# Optional: Enable debug logging (default: false)
# DEBUG=true
